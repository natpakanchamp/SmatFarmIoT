#define USER_SETUP_INFO "User_Setup"

// =================================================
// 1. เลือก Driver (ใช้ตัวเต็ม ST7789_DRIVER ดีกว่าตัว _2 ครับ)
// =================================================
#define ST7789_DRIVER      // ใช้ตัวนี้สำหรับจอ 1.54" 240x240

// =================================================
// 2. กำหนดขนาดจอและสี
// =================================================
#define TFT_WIDTH  240
#define TFT_HEIGHT 240

// จอ ST7789 ส่วนใหญ่ต้องสลับสีเป็น BGR และกลับสี (Invert)
#define TFT_RGB_ORDER TFT_BGR  // ถ้าสีแดงกับน้ำเงินสลับกัน ให้แก้บรรทัดนี้เป็น TFT_RGB
#define TFT_INVERSION_ON       // ถ้าจอเป็นสีขาวโพลน หรือสีเพี้ยน ให้ลองแก้เป็น TFT_INVERSION_OFF

// =================================================
// 3. กำหนด Offset (ตัวแก้ปัญหาจอเลื่อน/ภาพซ่า)
// =================================================
// สาเหตุที่ต้องใช้ 80 เพราะ Chip set 320 แต่จอจริง 240 (320-240 = 80)
// #define TFT_Y_OFFSET 80   // <--- บรรทัดที่ท่านถามถึง ใส่ไว้ตรงนี้ครับ

// =================================================
// 4. กำหนดขา Pin สำหรับ ESP32 (เช็คสายจริงของท่านด้วยนะครับ)
// =================================================
// ท่านต้องต่อสายตามนี้ หรือแก้ตัวเลขให้ตรงกับที่ท่านเสียบจริง
#define TFT_MISO -1   // ไม่ใช้ (จอพวกนี้ส่วนมากไม่มีขา MISO)
#define TFT_MOSI 23   // ต่อขา SDA ของจอ
#define TFT_SCLK 18   // ต่อขา SCL ของจอ
#define TFT_CS    5   // ต่อขา CS
#define TFT_DC    17   // ต่อขา DC (บางทีเขียนว่า RS)
#define TFT_RST   19   // ต่อขา RES (ถ้าต่อกับปุ่ม Reset ของบอร์ด ให้ใส่ -1)

// #define TFT_BL 32  // ถ้ามีขา BLK/Backlight ให้เปิดบรรทัดนี้และใส่ขา Pin

// =================================================
// 5. ตั้งค่าความถี่และความเร็ว
// =================================================
#define LOAD_GLCD   // โหลดฟอนต์มาตรฐาน
#define LOAD_FONT2
#define LOAD_FONT4
#define LOAD_FONT6
#define LOAD_FONT7
#define LOAD_FONT8
#define LOAD_GFXFF
#define SMOOTH_FONT

#define SPI_FREQUENCY  27000000 // 27MHz เสถียรสุดสำหรับ ST7789
#define SPI_READ_FREQUENCY  20000000
#define SPI_TOUCH_FREQUENCY  2500000
